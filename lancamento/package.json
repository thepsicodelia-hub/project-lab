{
  "name": "project-lab-lancamento",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4190 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4191 --strictPort",
    "package:preview": "npm run build && node scripts/preview.mjs"
  },
  "dependencies": {
    "three": "0.186.0"
  },
  "devDependencies": {
    "sharp": "^0.35.4",
    "vite": "8.3.0"
  }
}
